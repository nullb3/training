// initializing variables.

string firstName = "Hi! Your assigned key is: ";
Console.WriteLine(key);

// vars receive the following value and instantly interpret the implied data, so it must be initialized and assignet at once.

var key = 123

Console.Write(key);

// since it's an implied int, you cannot change it to anything else.

string name = "Bob";
int quantMessages = 3;
float temperature = 34.4f;

Console.Write("Hello, " + name + "! You have " + quantMessages + " messages in your inbox. The temperature is " + temperature + " celsius.");